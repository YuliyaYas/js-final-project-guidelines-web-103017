class EventListener {

  static activityListener(){
    const image = document.getElementsByClassName('left-side');
    debugger
    image.addEventListener("click", e => {
      let id = e.target.id;

    })
  };

}
